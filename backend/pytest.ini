[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -ra
markers =
    integration: tests that require live PostgreSQL/Redis (run inside Docker Compose)
filterwarnings =
    ignore::DeprecationWarning:langgraph.*
    ignore::PendingDeprecationWarning:langgraph.*
